$(document).ready(() => {
	// var str = "<tr>";
	// var columnNames= ["id","Latitude","Longitude","Image","Type","Name","Phone number","ReportDateTime","Status","Important","Urgent","Description"];
	// for (var i=0; i < columnNames.length; i++)
	// {
		// str += `<th>${columnNames[i]}</th>`;
	// }
	// str+="</tr>";
	// //$("#table").html(str);
	
});